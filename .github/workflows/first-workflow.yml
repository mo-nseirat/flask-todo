name: first work flow

on: 
    push: 
        branches:
            - "main"

jobs: 
    first-workflow-job:
        name: first job 
        runs-on: ubuntu-latest 
        steps:
            - name: run bash command 
              run: echo "First workflow"
              shell: bash 